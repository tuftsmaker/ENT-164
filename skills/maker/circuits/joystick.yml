board: freenove-esp32-wrover
title: KY-023 joystick — Freenove ESP32-WROVER (FNK0046)
subtitle: Two ADC axes plus a push button
components:
  - {type: joystick, id: joy, at: [3, c]}
wires:
  - {from: joy.GND, to: board.L14, color: black, label: GND}
  - {from: joy.+5V, to: board.L17, color: red, label: +5V — 3V3}
  - {from: joy.VRx, to: board.GPIO36, color: orange, label: VRx — GPIO 36}
  - {from: joy.VRy, to: board.GPIO39, color: yellow, label: VRy — GPIO 39}
  - {from: joy.SW, to: board.GPIO14, color: green, label: SW — GPIO 14}
notes:
  - "VRx and VRy are analog (ADC1 pins); SW is a button — use Pin.PULL_UP, it reads LOW when pressed."
  - "The +5V pin is happy on 3V3 here."
